# Ansible Inventory - Copy to inventory.ini and update with your VM IPs
# cp inventory.ini.example inventory.ini

[workers]
# Add your worker VMs here after terraform apply
# Format: name ansible_host=IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/your-key.pem
# worker1 ansible_host=1.2.3.4 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/axon-worker.pem

[workers:vars]
ansible_python_interpreter=/usr/bin/python3
repo_url=https://github.com/your-username/axon-browser-worker.git
repo_branch=main
